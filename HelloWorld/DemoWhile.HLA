program DemoWhile;
#include("stdlib.hhf")

static
    i: int8;

begin DemoWhile;

    mov(0, i);
    while(i < 10) do
        stdout.put("i=", i, nl);
        add(1, i);
    endwhile;
end DemoWhile;